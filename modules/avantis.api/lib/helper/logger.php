<?php

namespace Avantis\Api\Helper;

class Logger
{
    public static function add($query, $method, $request, $response, $start, int $time = 0)
    {
        return false; //\Logger::add($query, $method, $request, $response, $start, $time); // TODO воткнуть свой логер 
    }
}
